#include <stdio.h>
#include <stdlib.h>
/**
 * main - Program that print all numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
